생성형 AI를 배워보자

- 지금까지는 레이어를 쌓는걸 배웠음.
- 이제는 모델의 구조를 건드려보겠다.

이번 시간에 할 것.
flatten -> danse -> size down

예) 손글씨 데이터 28x28 -> 784x1 -> 32
분류하는 학습을 시키면 이 과정에서 이미지의 특징만 남은 필터가 됨.

그 다음, 다시 size up

32 -> 784x1 -> 28x28 하면 특징을 가진 것이 생성됨

